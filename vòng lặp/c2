// tong 1^2 + ... + n^2
#include <stdio.h>

int main(){
      int n;
      scanf("%d", &n);
      long long tong = 0;
      for(int i = 1; i <= n; i++){
            tong += (i * i);
      }
      printf("%lld", tong);
      return 0;
}
