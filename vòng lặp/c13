#include <stdio.h>
#include <math.h>

int main(){
      int n;
      scanf("%d", &n);
      long long tong = 0;
      for(int i = 1; i <= n; i++){
            tong += 2 * i - 1;
      }
      printf("%lld", tong);
      return 0;
}
